1. git lfs install as 'hdb_resale_with_coords.csv' is too large so it is in LFS warehouse
2. pip install -r requirements.txt to install libraries needed
3. run get_coords.ipynb to get the latitude and longitude for every transaction
4. run ML_Project_p2_.ipynb